// File: imgsoa/compress.hpp

#ifndef COMPRESS_HPP
#define COMPRESS_HPP
#include <string>

namespace common {
  int compress(const std::string& inputFilePath, const std::string& outputFilePath);
}

#endif //COMPRESS_HPP
